<%= form_for(@campsite) do |f| %>
  <% if @campsite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campsite.errors.count, "error") %> prohibited this campsite from being saved:</h2>

      <ul>
      <% @campsite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
